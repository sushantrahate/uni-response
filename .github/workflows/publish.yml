name: Publish to npm

on:
  push:
    tags:
      - "v*"

jobs:
  publish:
    runs-on: ubuntu-latest
    # Added permissions for OIDC (Required for modern npm security features)
    permissions:
      contents: read
      id-token: write 

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          registry-url: "https://registry.npmjs.org/"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Run tests
        run: npm test --if-present

      - name: Build
        run: npm run build --if-present

      # Trusted Publishing (OIDC) requires npm 11.5.0+; Node 20's bundled npm may be older.
      # Pinned to the npm 11.x line (not "latest") since npm 12+ requires Node 22.22.2+/24.15.0+/26+,
      # newer than the Node 20 this workflow runs on.
      - name: Update npm
        run: npm install -g npm@11

      - name: Publish to npm
        # No NPM_TOKEN needed: authenticates via GitHub OIDC against the
        # Trusted Publisher configured on the npmjs.com package settings.
        run: npm publish --provenance --access public
